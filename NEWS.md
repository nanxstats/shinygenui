# shinygenui 0.1.0

## New features

- Initial release: catalog-constrained generative UI for Shiny.
  Define components with `genui_component()`, collect them with
  `genui_catalog()`, and validate model-issued tool calls with the pure
  `genui_dispatch()` core.
