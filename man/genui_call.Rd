% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dispatch.R
\name{genui_call}
\alias{genui_call}
\title{Create a normalized tool call}
\usage{
genui_call(tool, args = list())
}
\arguments{
\item{tool}{Tool name: a component name from the catalog, or one of the
built-ins \code{"update_component"}, \code{"remove_component"}, \code{"clear_canvas"}.}

\item{args}{Named list of arguments as supplied by the model. For
\code{update_component}, \code{args} must contain \code{id} and \code{args} (the partial
arguments to merge, as a named list or a JSON object string).}
}
\value{
A \code{genui_call} object.
}
\description{
A \code{genui_call} is the normalized representation of one model-issued tool
call: the tool name plus its arguments as plain data. \code{\link[=genui_dispatch]{genui_dispatch()}}
consumes these. In production they are produced by the compiled ellmer
tools; in tests and replays you can hand-build them.
}
\examples{
genui_call("scatter_plot", list(x = "mpg", y = "hp"))
genui_call("update_component", list(id = "c1", args = list(x = "wt")))
genui_call("remove_component", list(id = "c1"))
}
