% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trace.R
\name{genui_replay}
\alias{genui_replay}
\title{Rebuild a canvas from a saved trace, no LLM required}
\usage{
genui_replay(
  trace,
  catalog,
  target,
  data = NULL,
  session = shiny::getDefaultReactiveDomain()
)
}
\arguments{
\item{trace}{A trace list, as returned by \code{\link[=genui_trace]{genui_trace()}} (already
evaluated, not the reactive) or restored via \code{\link[=readRDS]{readRDS()}}.}

\item{catalog}{The \code{\link[=genui_catalog]{genui_catalog()}} to render with; component names in
the trace must exist in it.}

\item{target}{Module id of the \code{\link[=genui_canvas]{genui_canvas()}} to rebuild into. Use a
canvas of its own, not one already driven by \code{\link[=genui_server]{genui_server()}}.}

\item{data}{Optional reactive (or function) returning the data object,
passed to component servers and \code{check()} hooks, as in
\code{\link[=genui_server]{genui_server()}}.}

\item{session}{The Shiny session (defaults to the current reactive
domain).}
}
\value{
(Invisibly) a list with reactives \code{trace} and \code{instances}
describing the rebuilt canvas, as in \code{\link[=genui_server]{genui_server()}}.
}
\description{
Folds a trace (from \code{\link[=genui_trace]{genui_trace()}}, or the \code{trace} reactive returned by
\code{\link[=genui_server]{genui_server()}}) through the same validate-and-execute pipeline the
model's tool calls use, recreating every instance in a \code{\link[=genui_canvas]{genui_canvas()}}
with id \code{target}. Instance ids come back identical because ids are
assigned deterministically and never reused. Embedded inputs return at
their default values: input state is ephemeral by design and not part of
the trace.
}
\details{
Entries that no longer validate (for example after the catalog changed)
are skipped with a warning; the rest of the trace still replays. The
target canvas is emptied first, so replaying twice is idempotent.
}
