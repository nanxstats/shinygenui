% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/component.R
\name{genui_component}
\alias{genui_component}
\title{Define a generative UI component}
\usage{
genui_component(
  name,
  description,
  args = list(),
  ui,
  server = NULL,
  check = NULL,
  container = FALSE,
  width = c("auto", "wide", "full")
)
}
\arguments{
\item{name}{Tool name the model sees. A snake_case string: lowercase
letters, digits, and underscores, starting with a letter. Must be unique
within a catalog and must not collide with the built-in lifecycle tools
(\code{update_component}, \code{remove_component}, \code{clear_canvas}).}

\item{description}{One to three sentences telling the model what the
component shows and when to use it. This is the model-facing
documentation for the component, so write it well.}

\item{args}{Named list of ellmer type specifications
(for example \code{\link[ellmer:type_string]{ellmer::type_string()}}, \code{\link[ellmer:type_enum]{ellmer::type_enum()}}) describing
the arguments the model may supply. As a shorthand, a plain string is
promoted to \verb{ellmer::type_string(<string>)}. Argument names \code{id} and
\code{parent_id} are reserved by the package. Catalogs are often built inside
the Shiny server function so enums can enumerate live facts, such as the
column names of the active dataset.}

\item{ui}{\verb{function(id, args)} returning an \code{\link[htmltools:tag]{htmltools::tag()}} (or tag
list). \code{id} is the fully namespaced module id for this instance; use
\code{shiny::NS(id)} to namespace any embedded inputs and outputs. \code{args} is
the validated argument list.}

\item{server}{Optional \verb{function(id, args, data)} that calls
\code{\link[shiny:moduleServer]{shiny::moduleServer()}} to wire outputs and embedded inputs. \code{data} is
the reactive passed to \code{\link[=genui_server]{genui_server()}}. If the module creates
observers, include them in the module's return value (alone or inside a
list) so the package can destroy them when the instance is updated or
removed. A \code{reactives} element in the return value is stored in the
instance registry keyed by instance id; nothing reads it in v0.1, it is
the hook for cross-component reactivity in a later release.}

\item{check}{Optional \verb{function(args, data)} for semantic validation
beyond the JSON schema. Return \code{NULL} when \code{args} are acceptable, or a
string describing the problem; the string is returned to the model as a
tool error so it can self-correct.}

\item{container}{If \code{TRUE}, this component can host child instances: its
\code{ui} must render an element with id \code{shiny::NS(id, "slot")}, and other
components may target it by passing the instance's id as \code{parent_id}.}

\item{width}{Layout hint for the canvas grid: \code{"auto"} (default),
\code{"wide"}, or \code{"full"}.}
}
\value{
A \code{genui_component} object.
}
\description{
A component is one entry in the finite catalog that constrains what the
model can render. It couples a model-facing tool schema (\code{name},
\code{description}, typed \code{args}) with developer-written rendering code: a
\code{ui} function plus an optional \code{server} function, instantiated together
as a dynamic Shiny module every time the model creates or updates an
instance. The model only ever supplies data arguments validated against
the declared types; it never emits code.
}
\examples{
genui_component(
  name = "note_card",
  description = "A card showing a short markdown note. Use for narrative
    text that should live on the canvas rather than in the chat.",
  args = list(
    title = "A short title for the card.",
    text = ellmer::type_string("The markdown body text.")
  ),
  ui = function(id, args) {
    htmltools::div(
      htmltools::h5(args$title),
      htmltools::p(args$text)
    )
  }
)
}
