% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/starter-pack.R
\name{genui_card_row}
\alias{genui_card_row}
\title{Container component: a titled row of cards}
\usage{
genui_card_row()
}
\value{
A \code{\link[=genui_component]{genui_component()}} object with \code{container = TRUE}.
}
\description{
A container the model can place other components into: create the row,
then create children with \code{parent_id} set to the row's instance id.
Children lay out in a responsive grid inside the row's card. Removing
the row removes its children. Add it to your \code{\link[=genui_catalog]{genui_catalog()}} alongside
\code{\link[=genui_components_bslib]{genui_components_bslib()}} (or your own components) to let the model
group related views.
}
\examples{
catalog <- genui_catalog(
  genui_card_row(),
  genui_component(
    name = "note_card",
    description = "A card showing a short note.",
    args = list(text = "The note text."),
    ui = function(id, args) htmltools::p(args$text)
  )
)
}
