% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/server.R
\name{genui_server}
\alias{genui_server}
\title{Server logic for a generative UI canvas}
\usage{
genui_server(
  id,
  catalog,
  chat,
  data = NULL,
  chat_id = NULL,
  greeting = NULL,
  system_prompt = NULL
)
}
\arguments{
\item{id}{Module id, matching the \code{\link[=genui_canvas]{genui_canvas()}} \code{id}.}

\item{catalog}{A \code{\link[=genui_catalog]{genui_catalog()}}.}

\item{chat}{An ellmer \code{Chat} object (any provider). Its system prompt is
replaced with \code{system_prompt}.}

\item{data}{A reactive (or function) returning the app's current data
object. It is passed to component \code{server()} functions and, isolated,
to \code{check()} hooks at validation time.}

\item{chat_id}{Id of a \code{\link[shinychat:chat_ui]{shinychat::chat_ui()}} placed in the UI at the
same namespace level as \code{\link[=genui_canvas]{genui_canvas()}}, or \code{NULL} (default) to manage
the chat loop yourself.}

\item{greeting}{Optional markdown string shown as the assistant's first
message (only used when \code{chat_id} is set). It is display-only and never
sent to the model.}

\item{system_prompt}{System prompt to install on \code{chat}. Defaults to
\code{\link[=genui_prompt]{genui_prompt()}} of the catalog; use \code{genui_prompt(catalog, context = ...)} to add app context, or pass any string to take full control.}
}
\value{
(Invisibly) a list with \code{chat} (the wired \code{Chat}), and the
reactives \code{trace} (the ordered call trace, see \code{genui_trace()}) and
\code{instances} (the live instance state, a named list keyed by id).
}
\description{
Wires a \code{\link[=genui_catalog]{genui_catalog()}} to an ellmer \code{Chat}: compiles every component
into a schema-validated tool, registers the built-in lifecycle tools
(\code{update_component}, \code{remove_component}, \code{clear_canvas}), installs the
assembled system prompt, and executes validated tool calls against the
\code{\link[=genui_canvas]{genui_canvas()}} with the matching \code{id}. Components stream onto the
canvas progressively as the model emits tool calls; validation and
rendering failures are returned to the model as tool errors and never
crash the session.
}
\details{
With \code{chat_id}, the package also runs the chat loop for a
\code{\link[shinychat:chat_ui]{shinychat::chat_ui()}} you placed in the UI: user input is streamed
through \code{chat$stream_async()} inside a \link[shiny:ExtendedTask]{shiny::ExtendedTask} (so other
sessions never block) and appended with \code{\link[shinychat:chat_append]{shinychat::chat_append()}},
with cancel support. With \code{chat_id = NULL} you run your own loop on
\code{chat}; the registered tools work all the same.

Create the \code{Chat} object inside your server function, one per session.
Sharing a single \code{Chat} across sessions would cross-wire the tool
closures and leak conversation history between users.
}
\section{Update semantics}{

\code{update_component} re-instantiates the component's module with the merged
arguments inside the instance's stable shell: same canvas position, same
ids, no page flicker. Because the module restarts, embedded input state
(like the starter histogram's bin slider) resets to its defaults on
update; snapshotting and restoring embedded input values across updates
is explicitly future work.
}

\section{Error feedback}{

Any failure while handling a tool call --- schema validation, a
component's \code{check()} hook, or a rendering error --- is signaled as a
regular R condition. ellmer catches it and returns
\code{conditionMessage()} to the model as the tool error, so the model can
correct its arguments and retry; the Shiny session itself never crashes,
and a failed call never leaves a half-rendered component behind.
Failures are always logged to the app's server log; set
\code{options(shinygenui.verbose = TRUE)} to also log successful canvas
operations.
}

